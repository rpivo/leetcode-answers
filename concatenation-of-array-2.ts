// define function getConcatenation, which takes in an array of numbers called
// nums, and returns an array of numbers
function getConcatenation(nums: number[]): number[] {
  // return nums concatenated with itself
  return nums.concat(nums);
}
